name: Gold Eye AI

on:
  schedule:
    - cron: '*/20 * * * *'  # 调整为20分钟一次，给数据接口喘息时间
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      # 🔥 核心升级：增加了 yfinance 库，用来抓取实时行情
      run: pip install requests feedparser yfinance
      
    - name: Run Gold Eye
      env: 
        PUSH_TOKEN: ${{ secrets.PUSH_TOKEN }}
        DEEPSEEK_KEY: ${{ secrets.DEEPSEEK_KEY }}
      run: python main.py
